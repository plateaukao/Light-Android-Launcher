<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- remove preference activity background color -->
    <!--    <style name="preferenceTheme" parent="PreferenceThemeOverlay.v14">-->
    <!--        <item name="android:windowBackground">@null</item>-->
    <!--    </style>-->

    <style name="TouchAreaDialog" parent="@android:style/Theme.DeviceDefault.Dialog.Alert">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorAlertDialogListItem">@android:color/black</item>
        <item name="android:backgroundDimAmount">0</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowBackground">@drawable/background_with_border</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>